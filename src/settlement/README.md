/settlement

This is the code ported from the central-settlements/src repo.

To make the migration easier to understand, we have elected to keep the settlements code in this 
subdirectory. In the future, we may wish to integrate the settlement code in closer, and merge the
directories together, e.g. `mv ./src/settlement/api/* ./src/api/`.

